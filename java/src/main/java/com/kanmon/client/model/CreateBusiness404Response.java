/*
 * Kanmon Public V2 API
 * Kanmon's public api. Contains all of the endpoints for both capital providers and platforms
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.kanmon.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.kanmon.client.model.BusinessNotFoundException;
import com.kanmon.client.model.CustomInitializationNotFoundException;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.kanmon.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class CreateBusiness404Response extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CreateBusiness404Response.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CreateBusiness404Response.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CreateBusiness404Response' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<BusinessNotFoundException> adapterBusinessNotFoundException = gson.getDelegateAdapter(this, TypeToken.get(BusinessNotFoundException.class));
            final TypeAdapter<CustomInitializationNotFoundException> adapterCustomInitializationNotFoundException = gson.getDelegateAdapter(this, TypeToken.get(CustomInitializationNotFoundException.class));

            return (TypeAdapter<T>) new TypeAdapter<CreateBusiness404Response>() {
                @Override
                public void write(JsonWriter out, CreateBusiness404Response value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `BusinessNotFoundException`
                    if (value.getActualInstance() instanceof BusinessNotFoundException) {
                        JsonElement element = adapterBusinessNotFoundException.toJsonTree((BusinessNotFoundException)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `CustomInitializationNotFoundException`
                    if (value.getActualInstance() instanceof CustomInitializationNotFoundException) {
                        JsonElement element = adapterCustomInitializationNotFoundException.toJsonTree((CustomInitializationNotFoundException)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: BusinessNotFoundException, CustomInitializationNotFoundException");
                }

                @Override
                public CreateBusiness404Response read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize BusinessNotFoundException
                    try {
                        // validate the JSON object to see if any exception is thrown
                        BusinessNotFoundException.validateJsonElement(jsonElement);
                        actualAdapter = adapterBusinessNotFoundException;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'BusinessNotFoundException'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for BusinessNotFoundException failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'BusinessNotFoundException'", e);
                    }
                    // deserialize CustomInitializationNotFoundException
                    try {
                        // validate the JSON object to see if any exception is thrown
                        CustomInitializationNotFoundException.validateJsonElement(jsonElement);
                        actualAdapter = adapterCustomInitializationNotFoundException;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'CustomInitializationNotFoundException'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for CustomInitializationNotFoundException failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'CustomInitializationNotFoundException'", e);
                    }

                    if (match == 1) {
                        CreateBusiness404Response ret = new CreateBusiness404Response();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CreateBusiness404Response: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public CreateBusiness404Response() {
        super("oneOf", Boolean.FALSE);
    }

    public CreateBusiness404Response(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("BusinessNotFoundException", BusinessNotFoundException.class);
        schemas.put("CustomInitializationNotFoundException", CustomInitializationNotFoundException.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return CreateBusiness404Response.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * BusinessNotFoundException, CustomInitializationNotFoundException
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof BusinessNotFoundException) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof CustomInitializationNotFoundException) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be BusinessNotFoundException, CustomInitializationNotFoundException");
    }

    /**
     * Get the actual instance, which can be the following:
     * BusinessNotFoundException, CustomInitializationNotFoundException
     *
     * @return The actual instance (BusinessNotFoundException, CustomInitializationNotFoundException)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `BusinessNotFoundException`. If the actual instance is not `BusinessNotFoundException`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `BusinessNotFoundException`
     * @throws ClassCastException if the instance is not `BusinessNotFoundException`
     */
    public BusinessNotFoundException getBusinessNotFoundException() throws ClassCastException {
        return (BusinessNotFoundException)super.getActualInstance();
    }
    /**
     * Get the actual instance of `CustomInitializationNotFoundException`. If the actual instance is not `CustomInitializationNotFoundException`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `CustomInitializationNotFoundException`
     * @throws ClassCastException if the instance is not `CustomInitializationNotFoundException`
     */
    public CustomInitializationNotFoundException getCustomInitializationNotFoundException() throws ClassCastException {
        return (CustomInitializationNotFoundException)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to CreateBusiness404Response
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with BusinessNotFoundException
        try {
            BusinessNotFoundException.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for BusinessNotFoundException failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with CustomInitializationNotFoundException
        try {
            CustomInitializationNotFoundException.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for CustomInitializationNotFoundException failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for CreateBusiness404Response with oneOf schemas: BusinessNotFoundException, CustomInitializationNotFoundException. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of CreateBusiness404Response given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of CreateBusiness404Response
     * @throws IOException if the JSON string is invalid with respect to CreateBusiness404Response
     */
    public static CreateBusiness404Response fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, CreateBusiness404Response.class);
    }

    /**
     * Convert an instance of CreateBusiness404Response to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

